node {
    stage('Configuration') {
        properties([pipelineTriggers([[$class: 'GitHubPushTrigger'], pollSCM('H/15 * * * *')])])
        checkout scm
        echo 'Deploying elixar-web prod scripted....'
    }
    stage('Preparation') {
            echo 'Deploying elixar-web prod scripted....'
    }
    stage ("Install dependencies") {
            echo 'Deploying elixar-web prod scripted....'
    }
    stage('Build') {
            echo 'Deploying elixar-web prod scripted....'
    }
}
